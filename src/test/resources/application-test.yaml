spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: custom_schema
  liquibase:
    enabled: true
    default-schema: custom_schema
    liquibase-schema: custom_schema